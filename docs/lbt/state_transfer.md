# State Transfer

Instructions for moving data between GPRs, LBT scratch registers, and flag registers.

{{ lbt_page('state_transfer') }}

**Status:** Tested on real LoongArch64 hardware by `code/lbt`.
