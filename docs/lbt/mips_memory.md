# MIPS Unaligned Memory Helpers

LBT provides MIPS-style unaligned load/store helpers (`ldl`/`ldr`/`stl`/`str`).

{{ lbt_page('mips_memory') }}

**Status:** Tested on real LoongArch64 hardware by `code/lbt`.
