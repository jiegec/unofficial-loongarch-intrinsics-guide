# LBT x86 Arithmetic Flags

The `x86*` arithmetic helpers are flags-only instructions: source GPRs are preserved and x86-visible flags are updated from the computed result.

{{ lbt_page('x86_arithmetic') }}

